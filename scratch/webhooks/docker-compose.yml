services:
  webhook-sink:
    image: nginx:1.27-alpine
    container_name: klaviyo-webhook-sink
    ports:
      - "8090:8090"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./logs:/var/log/nginx
    restart: unless-stopped
